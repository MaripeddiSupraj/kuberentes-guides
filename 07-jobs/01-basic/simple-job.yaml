apiVersion: batch/v1
kind: Job
metadata:
  name: data-processor
spec:
  template:
    spec:
      containers:
      - name: processor
        image: busybox:1.35
        command: ['sh', '-c', 'echo "Processing data..." && sleep 30 && echo "Data processing complete!"']
      restartPolicy: Never
  backoffLimit: 4